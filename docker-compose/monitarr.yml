version: "3"
services:
  app:
    image: ghcr.io/nickshanks347/monitarr:latest@sha256:ab6a77122e16e13aa99cefc85f975f1eda2cfe0ae0c6bb888a755244dcb5b63b
    ports:
      - ${PORT:-3000}:3000
    environment:
      - SONARR_URL=$SONARRURL
      - SONARR_API_KEY=$SONARRAPI
      - RADARR_URL=$RADARRURL
      - RADARR_API_KEY=$RADARRAPI
      - BASE_PATH=${CFURLPATH:-}
    restart: unless-stopped
#    healthcheck:
#      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://127.0.0.1:3000$${CFURLPATH:-} || exit 1"]
#      interval: 30s
#      timeout: 20s
#      retries: 3
#      start_period: 30s
    labels:
      #- autoheal=true
      - dfgeneric.enable=${CFTUNNEL:-true}
      - dfgeneric.0.hostname=${CFSUBDOMAIN}${CFDOMAIN}
      - dfgeneric.0.service=http://${HOSTNAME:-localhost}:${PORT:-3000}
      - dfgeneric.0.access.policy=${CFPOLICY:-default_tld}
      - dfgeneric.0.zonename=${CFDOMAIN}
      - dfgeneric.0.path=${CFURLPATH:-}
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3" 

